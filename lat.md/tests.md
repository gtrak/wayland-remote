# Tests

Test specifications for wayland-remote.

Leaf sections (individual test specs with `// @lat:` refs in code) are added as tests land, issue by issue — see the active plans in `docs/plans/`. This file intentionally carries no leaf sections until the first tests exist; the `require-code-mention` frontmatter returns with the first real spec sections (first added in plan 001, issue 02).
