---
phase: 02-wayland-core-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "crates/server/Cargo.toml",
  "crates/server/src/state.rs",
  "crates/server/src/main.rs"
]
autonomous: true
requirements:
  - WAYL-01
must_haves:
  truths:
    - "Wayland socket is created and accepts client connections"
    - "CompositorState is initialized and wl_compositor global is advertised"
    - "calloop event loop integrates Display and dispatches client requests"
  artifacts:
    - path: "crates/server/src/state.rs"
      provides: "ServerState struct with CompositorState, Display integration"
      exports: ["ServerState", "ClientState"]
    - path: "crates/server/Cargo.toml"
      provides: "Wayland dependencies"
      contains: "smithay = { version = \"0.7.0\", features = [\"wayland_frontend\"] }"
  key_links:
    - from: "crates/server/src/main.rs"
      to: "crates/server/src/state.rs"
      via: "ServerState::new() call"
      pattern: "ServerState::new"
---

<objective>
Initialize the core Wayland compositor following the Smallvil pattern: set up CompositorState with wayland_frontend feature, create Display integrated with calloop event loop, and accept client connections via ListeningSocketSource. This establishes the foundation for all Wayland protocol handling.

Purpose: Without the core compositor infrastructure, no Wayland protocol can be handled and no clients can connect. This is the absolute minimum viable compositor.
Output: Working server binary that advertises wl_compositor global and accepts client connections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-wayland-core-protocol/02-RESEARCH.md

The project already has:
- Virtual workspace with server/viewer crates
- Placeholder main.rs using tokio
- Cargo.toml with smithay pinned at 0.7.0 (no features enabled yet)

This plan builds on Phase 1 infrastructure and follows the Smallvil pattern from research.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wayland dependencies to server Cargo.toml</name>
  <files>crates/server/Cargo.toml</files>
  <action>
Add Wayland server dependencies following the Smallvil pattern:
1. Enable "wayland_frontend" feature for smithay
2. Add calloop = "0.14.0" for event loop
3. Add wayland-server = "0.31.9" for Display handle
4. Add wayland-protocols = "0.32.8" for protocol definitions

DO NOT use full smithay features yet - just wayland_frontend for Phase 2. Keep tokio in dev-dependencies for Phase 4 integration.

Verify smithay version stays pinned at 0.7.0 exactly.
  </action>
  <verify>
    <automated>cat crates/server/Cargo.toml | grep -E "(smithay|calloop|wayland-server)" | head -20</automated>
  </verify>
  <done>Cargo.toml contains smithay with wayland_frontend feature, calloop 0.14.0, and wayland-server 0.31.9</done>
</task>

<task type="auto">
  <name>Task 2: Create ServerState struct with Smithay states</name>
  <files>crates/server/src/state.rs</files>
  <action>
Create crates/server/src/state.rs implementing the Smallvil pattern:

1. Define ServerState struct holding:
   - display_handle: DisplayHandle
   - compositor_state: CompositorState
   - socket_name: OsString

2. Implement ServerState::new(event_loop, display) that:
   - Creates CompositorState::new::<Self>(&dh) 
   - Sets up ListeningSocketSource::new_auto()
   - Inserts socket source into calloop event loop
   - Inserts Display source into calloop event loop using Generic::new()
   - Returns socket_name for client connection

3. Define ClientState struct implementing ClientData trait:
   - compositor_state: CompositorClientState
   - Default impl for ClientData with empty initialized/disconnected

Use delegate_compositor!(ServerState) macro to generate boilerplate.

Reference: Smallvil pattern from 02-RESEARCH.md lines 94-176
  </action>
  <verify>
    <automated>rustc --crate-type lib --check crates/server/src/state.rs 2>&1 | head -20 || echo "Syntax check complete"</automated>
  </verify>
  <done>state.rs exists with ServerState containing CompositorState, ListeningSocketSource setup, ClientState with ClientData impl</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite main.rs to use calloop event loop</name>
  <files>crates/server/src/main.rs</files>
  <action>
Replace the tokio placeholder main.rs with calloop-based Wayland compositor:

1. Remove tokio::main, use standard main()
2. Initialize tracing subscriber
3. Create calloop::EventLoop<ServerState>
4. Create wayland_server::Display<ServerState>
5. Call ServerState::new() to initialize compositor and socket
6. Print socket path: /run/user/{uid}/{socket_name}
7. Run event_loop.run(None, &mut state, |_| {})

Keep anyhow::Result return type. Add error handling for all init steps.

The main loop should block and dispatch Wayland events forever.
  </action>
  <verify>
    <automated>cargo check --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>main.rs compiles with calloop event loop, ServerState initialization, and Wayland socket creation</done>
</task>

</tasks>

<verification>
- [ ] `cargo build --package wayland-remote-server` succeeds
- [ ] Binary starts and prints Wayland socket path
- [ ] Socket file exists in /run/user/{uid}/wayland-{N}
- [ ] WAYLAND_DISPLAY env var documented for clients
</verification>

<success_criteria>
Measurable completion:
1. Server binary compiles and runs without errors
2. Wayland socket is created and listening
3. wl_compositor global is advertised (verifiable with wayland-info tool)
4. Event loop processes client connections (test with simple-connect.c or wayland-client)
5. No memory leaks or crashes on startup
</success_criteria>

<output>
After completion, create `.planning/phases/02-wayland-core-protocol/02-01-SUMMARY.md` documenting:
- Socket path format and environment setup
- ServerState structure and key components
- Event loop integration pattern used
