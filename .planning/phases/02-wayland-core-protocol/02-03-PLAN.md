---
phase: 02-wayland-core-protocol
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: [
  "crates/server/src/handlers/compositor.rs",
  "crates/server/src/state.rs",
  "crates/server/tests/test_surface_lifecycle.rs",
  "crates/server/Cargo.toml"
]
autonomous: true
requirements:
  - WAYL-02
  - WAYL-03
must_haves:
  truths:
    - "Surface commits trigger CompositorHandler::commit callback"
    - "Buffer attachments are detected and tracked"
    - "Surface destruction releases resources without leaks"
    - "Test client can create surface, attach buffer, commit, and destroy"
  artifacts:
    - path: "crates/server/src/handlers/compositor.rs"
      provides: "CompositorHandler implementation with commit tracking"
      exports: ["CompositorHandler", "commit callback"]
    - path: "crates/server/tests/test_surface_lifecycle.rs"
      provides: "Integration test for surface lifecycle"
      contains: "test_surface_create_attach_commit_destroy"
    - path: "crates/server/src/state.rs"
      provides: "Surface tracking and destruction hooks"
      contains: "add_destruction_hook"
  key_links:
    - from: "crates/server/src/handlers/compositor.rs"
      to: "crates/server/src/state.rs"
      via: "commit callback accesses ServerState"
      pattern: "fn commit.*surface.*WlSurface"
---

<objective>
Implement CompositorHandler to track surface lifecycle: create, buffer attach, commit, and destruction. This satisfies WAYL-02 (surface operations) and WAYL-03 (cleanup). Add integration tests using wayland-client to verify the full surface lifecycle works end-to-end. This is the final piece of Phase 2 core protocol support.

Purpose: Surface lifecycle is the heart of Wayland - clients create surfaces, attach buffers (pixel data), commit changes, and eventually destroy them. Without proper handling, clients will freeze, crash, or leak memory.
Output: Working surface lifecycle with test coverage, completing Phase 2 requirements.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wayland-core-protocol/02-01-SUMMARY.md
@.planning/phases/02-wayland-core-protocol/02-02-SUMMARY.md
@.planning/phases/02-wayland-core-protocol/02-RESEARCH.md

This plan depends on both previous plans and adds the critical surface management layer:
- CompositorHandler::commit() callback for surface commits
- Buffer attachment detection via SurfaceAttributes
- Destruction hooks for cleanup
- Integration testing with wayland-client

Key insight from research: Smithay handles most surface details; we just need to implement the commit callback and track what's happening.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CompositorHandler with commit tracking</name>
  <files>crates/server/src/handlers/compositor.rs, crates/server/src/handlers/mod.rs</files>
  <action>
Create crates/server/src/handlers/compositor.rs implementing CompositorHandler:

1. Implement CompositorHandler trait for ServerState with:
   - compositor_state(&mut self) -> &mut CompositorState
   - client_compositor_state(&self, client: &Client) -> &CompositorClientState
   - commit(&mut self, surface: &WlSurface) - called on every commit

2. In commit() callback:
   - Use smithay::wayland::compositor::with_states() to access surface data
   - Access SurfaceAttributes via surface_data.cached_state.current::<SurfaceAttributes>()
   - Check attrs.buffer for BufferAssignment (new buffer attached)
   - Log/tracing::info when buffer is attached (for debugging)
   - Store surface ID in ServerState for tracking (HashMap<WlSurface, SurfaceInfo>)

3. Add delegate_compositor!(ServerState) macro

4. Update handlers/mod.rs to export compositor module

5. Add to ServerState in state.rs:
   - surfaces: HashMap<ObjectId, SurfaceInfo> (track active surfaces)
   - SurfaceInfo struct with creation time, buffer count, etc.

Reference: Research lines 188-217 for CompositorHandler pattern. Use tracing to log surface activity.
  </action>
  <verify>
    <automated>cargo check --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>compositor.rs exists with CompositorHandler impl, commit() callback accesses SurfaceAttributes, surfaces tracked in ServerState</done>
</task>

<task type="auto">
  <name>Task 2: Add destruction hooks and ShmState</name>
  <files>crates/server/src/state.rs</files>
  <action>
Update state.rs for complete surface lifecycle (WAYL-03):

1. Add ShmState for shared memory buffers:
   - shm_state: ShmState in ServerState
   - Initialize ShmState::new::<Self>(&dh, vec![]) in ::new()
   - Add delegate_shm!(ServerState) macro

2. Add surface destruction hooks in ServerState::new():
   - Use compositor_state.add_destruction_hook(move |surface| { ... })
   - Remove surface from surfaces HashMap
   - Log destruction event

3. Update ServerState struct to include:
   - shm_state: ShmState
   - Add ShmHandler trait impl (minimal)

4. Ensure all delegate macros are present:
   - delegate_compositor!(ServerState)
   - delegate_seat!(ServerState)
   - delegate_shm!(ServerState)
   - (delegate_output if needed by Smithay)

5. Verify all imports are present for destruction hooks

Reference: Research mentions ShmState is needed for clients to create SHM buffers. Research lines 302-305 mention buffer release timing - note this for Phase 3.
  </action>
  <verify>
    <automated>cargo build --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>ShmState added, destruction hooks registered, all delegate macros present, builds successfully</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test for surface lifecycle</name>
  <files>crates/server/tests/test_surface_lifecycle.rs, crates/server/Cargo.toml</files>
  <action>
Create comprehensive integration test for WAYL-02 and WAYL-03:

1. Update Cargo.toml dev-dependencies:
   - wayland-client = "0.31"
   - wayland-protocols = "0.32" (client feature)
   - tempfile = "3" (for test socket path)

2. Create crates/server/tests/test_surface_lifecycle.rs:

   a. test_surface_create():
      - Connect to compositor socket
      - Get wl_compositor global
      - Create wl_surface
      - Verify surface creation succeeds

   b. test_surface_commit():
      - Create surface
      - Commit surface (empty commit)
      - Verify no errors

   c. test_surface_attach_commit():
      - Create surface
      - Create SHM pool and buffer (wayland-client shm API)
      - Attach buffer to surface
      - Commit surface
      - Verify server receives commit (via tracing or hook)

   d. test_surface_destroy():
      - Create, attach, commit surface
      - Destroy surface
      - Verify destruction hook called

3. Use wayland-client event queue to dispatch events
4. Set WAYLAND_DISPLAY env var to test socket
5. Add test timeout (30 seconds) to prevent hangs

Reference: Research lines 384-391 for test approach. Follow wayland-client crate patterns.

6. Add test_common module with helper functions for:
   - Connecting to test compositor
   - Creating SHM buffers with dummy data
   - Waiting for server events
  </action>
  <verify>
    <automated>cargo test --package wayland-remote-server --test test_surface_lifecycle -- --test-threads=1 2>&1 | tail -50</automated>
  </verify>
  <done>Test file exists, compiles, and tests pass showing surface create/attach/commit/destroy lifecycle works</done>
</task>

</tasks>

<verification>
- [ ] `cargo test --package wayland-remote-server` passes
- [ ] Surface creation test: creates wl_surface successfully
- [ ] Surface commit test: commit callback invoked
- [ ] Surface attach test: buffer attachment detected
- [ ] Surface destroy test: destruction hook called, no memory leaks
- [ ] Server runs without errors with test clients
</verification>

<success_criteria>
Measurable completion:
1. Test client using wayland-client connects and creates surface
2. Buffer attach is detected in commit() callback (logged via tracing)
3. Surface destruction removes from tracking HashMap
4. All tests pass: `cargo test --package wayland-remote-server -- --test-threads=1`
5. valgrind or similar shows no memory leaks (optional but recommended)
6. Phase 2 requirements satisfied:
   - WAYL-01: ✓ (wl_compositor, wl_seat, wl_output, wl_shm available)
   - WAYL-02: ✓ (surfaces created, buffers attached, commits handled)
   - WAYL-03: ✓ (destruction hooks cleanup resources)
</success_criteria>

<output>
After completion, create `.planning/phases/02-wayland-core-protocol/02-03-SUMMARY.md` documenting:
- Surface lifecycle implementation
- Test coverage and approach
- Known limitations (rendering in Phase 3)
- Key lessons about Smithay patterns
- Phase 2 completion status

Update .planning/STATE.md Phase 2 status to complete.
