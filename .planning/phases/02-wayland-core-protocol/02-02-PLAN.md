---
phase: 02-wayland-core-protocol
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "crates/server/src/state.rs",
  "crates/server/src/handlers/seat.rs",
  "crates/server/src/handlers/output.rs",
  "crates/server/src/handlers/mod.rs"
]
autonomous: true
requirements:
  - WAYL-01
must_haves:
  truths:
    - "wl_seat global advertised with keyboard and pointer capabilities"
    - "wl_output global advertised with virtual display mode"
    - "Per-client state tracks compositor and seat resources"
  artifacts:
    - path: "crates/server/src/handlers/seat.rs"
      provides: "SeatHandler implementation with wl_seat global"
      exports: ["SeatHandler", "seat initialization"]
    - path: "crates/server/src/handlers/output.rs"
      provides: "Virtual output with 1920x1080 @ 60Hz mode"
      exports: ["create_virtual_output"]
    - path: "crates/server/src/handlers/mod.rs"
      provides: "Handler module organization"
  key_links:
    - from: "crates/server/src/state.rs"
      to: "crates/server/src/handlers/seat.rs"
      via: "SeatHandler trait implementation"
      pattern: "impl SeatHandler for ServerState"
---

<objective>
Add wl_seat and wl_output globals to enable complete Wayland client support. The seat provides input device advertisement (keyboard, pointer) that clients require to create windows. The virtual output provides display information so clients know where to render. Both are required for most Wayland applications to initialize successfully.

Purpose: Without wl_seat, clients cannot receive input focus. Without wl_output, clients don't know display parameters. These globals complete the core protocol foundation.
Output: Server with full WAYL-01 compliance: wl_compositor, wl_seat, wl_output, and wl_surface all functional.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wayland-core-protocol/02-01-SUMMARY.md
@.planning/phases/02-wayland-core-protocol/02-RESEARCH.md

This plan depends on Plan 01 (core compositor setup). It adds:
- SeatState for wl_seat global and input device management
- OutputManagerState and virtual Output for wl_output
- Enhanced ClientState for per-client tracking

Smithay automatically manages seat and output protocol details; we just initialize the globals.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seat handler with keyboard and pointer</name>
  <files>crates/server/src/handlers/seat.rs, crates/server/src/handlers/mod.rs</files>
  <action>
Create crates/server/src/handlers/seat.rs implementing SeatHandler:

1. Add to ServerState in state.rs:
   - seat_state: SeatState<ServerState>
   - seat: Seat<ServerState>

2. In ServerState::new(), after compositor_state init:
   - Initialize SeatState::new()
   - Create seat: seat_state.new_wl_seat(&dh, "wayland-remote-seat")
   - Add keyboard: seat.add_keyboard(Default::default(), 200, 25).unwrap()
   - Add pointer: seat.add_pointer()

3. Implement SeatHandler trait for ServerState (minimal - just return seat_state reference)

4. Use delegate_seat!(ServerState) macro

5. Create handlers/mod.rs to export seat module

Reference: Research lines 119-131 for seat initialization pattern
  </action>
  <verify>
    <automated>cargo check --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>seat.rs exists with SeatHandler impl, ServerState has seat_state and seat, keyboard/pointer added</done>
</task>

<task type="auto">
  <name>Task 2: Create virtual output handler</name>
  <files>crates/server/src/handlers/output.rs</files>
  <action>
Create crates/server/src/handlers/output.rs with virtual output:

1. Add to ServerState in state.rs:
   - output_manager_state: OutputManagerState
   - output: Output (optional, can be stored separately)

2. In ServerState::new(), after seat init:
   - Initialize OutputManagerState::new_with_xdg_output::<Self>(&dh)
   - Create virtual output using smithay::output::Output::new()
   - Set PhysicalProperties with size (0,0) for headless
   - Set make: "Wayland Remote", model: "Virtual"
   - Call output.create_global::<ServerState>(&dh) to advertise
   - Configure mode: 1920x1080 @ 60Hz (60000 mHz)
   - Set Transform::Normal, Scale::Integer(1)

3. Export output configuration from handlers/output.rs

Reference: Research lines 229-259 for output creation pattern. This is CRITICAL - missing wl_output causes silent client failures per Pitfall 5.
  </action>
  <verify>
    <automated>cargo check --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>output.rs exists, ServerState has output_manager_state and virtual output, wl_output global advertised</done>
</task>

<task type="auto">
  <name>Task 3: Update ClientState and verify globals</name>
  <files>crates/server/src/state.rs</files>
  <action>
Enhance state.rs to complete the protocol setup:

1. Update ClientState to include:
   - compositor_state: CompositorClientState (existing)
   - seat_state: SeatClientState (new)

2. Ensure ServerState imports include:
   - smithay::wayland::seat::{Seat, SeatState}
   - smithay::wayland::output::{Output, OutputManagerState}
   - smithay::output::{PhysicalProperties, Mode, Subpixel}
   - smithay::utils::Transform
   - smithay::utils::Scale

3. Update ServerState::new() to initialize all states in order:
   - CompositorState
   - SeatState + Seat + keyboard + pointer
   - OutputManagerState + Output
   - ListeningSocketSource

4. Add #[derive(Default)] to ClientState

5. Verify delegate macros at end of state.rs:
   - delegate_compositor!(ServerState)
   - delegate_seat!(ServerState)
   - delegate_output!(ServerState) [if needed - check Smithay docs]

6. Run cargo build to verify no errors
  </action>
  <verify>
    <automated>cargo build --package wayland-remote-server 2>&1 | tail -30</automated>
  </verify>
  <done>Server builds with all globals, ClientState has seat tracking, all delegate macros present</done>
</task>

</tasks>

<verification>
- [ ] `cargo build --package wayland-remote-server` succeeds
- [ ] Server starts and advertises wl_seat (check with WAYLAND_DISPLAY=wayland-1 wayland-info 2>/dev/null || echo "wayland-info not available")
- [ ] Server advertises wl_output with 1920x1080 mode
- [ ] All globals: wl_compositor, wl_seat, wl_output, wl_shm (if ShmState added)
</verification>

<success_criteria>
Measurable completion:
1. wayland-info or similar tool lists wl_seat global
2. wayland-info lists wl_output with 1920x1080 @ 60Hz mode
3. Simple Wayland client (weston-info, wayland-info) connects successfully
4. Client reports available seat and output
5. No errors in server logs about missing globals
</success_criteria>

<output>
After completion, create `.planning/phases/02-wayland-core-protocol/02-02-SUMMARY.md` documenting:
- Seat and output configuration
- Mode settings (1920x1080 @ 60Hz)
- Client state tracking
- Verification commands used
