---
phase: 04-tcp-frame-streaming
plan: '03'
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - crates/server/src/streaming/surface.rs
  - crates/server/src/state.rs
autonomous: true
requirements: [STREAM-04]

must_haves:
  truths:
    - "Each Wayland surface maps to unique window ID"
    - "Multiple surfaces stream independently with correct IDs"
    - "Surface destruction removes from streaming state"
  artifacts:
    - path: "crates/server/src/streaming/surface.rs"
      provides: "Surface tracking and window ID management"
      exports: ["SurfaceTracker", "allocate_window_id", "remove_surface"]
    - path: "crates/server/src/state.rs"
      provides: "Surface tracker integrated with streaming state"
      contains: "surface_tracker"
  key_links:
    - from: "crates/server/src/streaming/surface.rs"
      to: "crates/server/src/state.rs"
      via: "ServerState captured_frames access"
      pattern: "captured_frames.*ObjectId"
    - from: "crates/server/src/streaming/client.rs"
      to: "crates/server/src/streaming/surface.rs"
      via: "SurfaceTracker for window_id mapping"
      pattern: "SurfaceTracker.*window_id"
---

<objective>
Implement multi-surface tracking with unique window IDs for streaming.

Purpose: Enable streaming of multiple Wayland surfaces simultaneously with distinct identifiers
Output: surface.rs with SurfaceTracker, updated state.rs
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-tcp-frame-streaming/04-02-PLAN.md
@.planning/phases/03-headless-rendering/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SurfaceTracker for window ID management</name>
  <files>crates/server/src/streaming/surface.rs</files>
  <action>
Create surface.rs for multi-surface tracking:

1. Create SurfaceTracker struct:
   - next_window_id: AtomicU32 - incrementing counter for unique IDs
   - surface_to_window: RwLock<HashMap<ObjectId, u32>> - Wayland surface -> window ID
   - window_to_surface: RwLock<HashMap<u32, ObjectId>> - window ID -> Wayland surface
2. Implement methods:
   - allocate_window_id(surface_id: ObjectId) -> u32 - maps new surface to unique ID
   - get_window_id(surface_id: ObjectId) -> Option<u32> - lookup existing mapping
   - remove_surface(surface_id: ObjectId) - cleanup on surface destruction
3. Use parking_lot RwLock (faster than tokio::sync for local state)

Reference Phase 3 SUMMARY: surfaces are tracked by ObjectId in captured_frames.
  </action>
  <verify>
<automated>cargo check -p wayland-remote-server 2>&1 | head -30</automated>
</verify>
  <done>SurfaceTracker provides unique window IDs for each Wayland surface</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SurfaceTracker with ServerState</name>
  <files>crates/server/src/state.rs</files>
  <action>
Update ServerState to include SurfaceTracker:

1. Add surface_tracker: Arc<SurfaceTracker> field to StreamingState
2. Modify commit handler in state.rs:
   - When new surface commits, call allocate_window_id()
   - Pass window_id to frame extraction for streaming
3. Modify surface destruction handler:
   - Call remove_surface() to clean up window ID mapping
4. Update get_frames_for_streaming():
   - Use SurfaceTracker to map ObjectId -> window_id
   - Include window_id in FrameHeader

Reference Phase 3 SUMMARY: commit() already handles RGBA extraction to captured_frames.
  </action>
  <verify>
<automated>cargo check -p wayland-remote-server 2>&1 | head -30</automated>
</verify>
  <done>ServerState integrates SurfaceTracker for multi-surface streaming</done>
</task>

</tasks>

<verification>
[Phase 4 checks]
- Multiple surfaces can stream simultaneously
- Each surface has unique window ID
- Surface destruction removes from streaming
</verification>

<success_criteria>
- Multiple surfaces tracked with unique window IDs (STREAM-04)
- Server handles disconnections without crash (STREAM-04)
- All 4 requirements complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-tcp-frame-streaming/04-03-SUMMARY.md`
</output>
