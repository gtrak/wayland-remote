---
phase: 04-tcp-frame-streaming
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/server/src/streaming/mod.rs
  - crates/server/src/streaming/protocol.rs
  - crates/server/src/state.rs
autonomous: true
requirements: [STREAM-01, STREAM-02]

must_haves:
  truths:
    - "TCP server can bind to configurable port"
    - "Frame protocol defines 20-byte header with window_id, width, height, timestamp"
    - "RGBA payload follows header with correct byte order (big-endian)"
  artifacts:
    - path: "crates/server/src/streaming/mod.rs"
      provides: "TCP server lifecycle, Tokio integration with calloop"
      exports: ["StreamingServer", "start_streaming_server"]
    - path: "crates/server/src/streaming/protocol.rs"
      provides: "Binary frame protocol encoding/decoding"
      exports: ["FrameHeader", "encode_frame", "decode_header"]
    - path: "crates/server/src/state.rs"
      provides: "StreamingState integration with ServerState"
      contains: "StreamingState"
  key_links:
    - from: "crates/server/src/streaming/mod.rs"
      to: "crates/server/src/state.rs"
      via: "StreamingState shared via Arc"
      pattern: "Arc.*StreamingState"
    - from: "crates/server/src/streaming/protocol.rs"
      to: "tokio::io"
      via: "AsyncWriteExt write_all"
      pattern: "write_u32.*write_u64"
---

<objective>
Create streaming module foundation with binary protocol definition and TCP server skeleton.

Purpose: Establish the networking layer that will stream captured frames to Windows viewers
Output: streaming/mod.rs, streaming/protocol.rs, updated state.rs
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tcp-frame-streaming/04-RESEARCH.md
@.planning/phases/03-headless-rendering/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create streaming module structure and protocol</name>
  <files>crates/server/src/streaming/mod.rs, crates/server/src/streaming/protocol.rs</files>
  <action>
Create streaming/ directory with mod.rs and protocol.rs:

1. Create crates/server/src/streaming/mod.rs with:
   - Module exports for protocol, client, surface submodules
   - StreamingServer struct with port configuration
   - start_streaming_server() async function using Tokio TcpListener
   - calloop::futures integration pattern from RESEARCH.md

2. Create crates/server/src/streaming/protocol.rs with:
   - FrameHeader struct: window_id (u32), width (u32), height (u32), timestamp_us (u64)
   - encode_frame() - writes header then RGBA payload to BytesMut
   - decode_header() - parses 20-byte header from buffer
   - Use big-endian byte order throughout (u32::to_be_bytes, u64::to_be_bytes)

DO NOT implement actual frame sending yet - that comes in Plan 02.
  </action>
  <verify>
<automated>cargo check -p wayland-remote-server 2>&1 | head -30</automated>
</verify>
  <done>streaming/mod.rs exists with StreamingServer struct, protocol.rs has FrameHeader with encode/decode</done>
</task>

<task type="auto">
  <name>Task 2: Add streaming state to ServerState</name>
  <files>crates/server/src/state.rs</files>
  <action>
Integrate streaming state into existing ServerState:

1. Add import for streaming module
2. Add streaming_state: Arc<RwLock<StreamingState>> field to ServerState
3. Create StreamingState struct with:
   - surfaces: Arc<RwLock<HashMap<u32, FrameData>>> - window_id -> frame data
   - clients: Arc<RwLock<HashMap<SocketAddr, ClientHandle>>> - connected viewers
   - port: u16 - configurable streaming port
4. Add methods:
   - get_frames_for_streaming() - iterates captured_frames, maps ObjectId to window_id
   - update_frame(window_id, RgbaData) - updates frame data for streaming

Reference Phase 3 SUMMARY: captured_frames provides HashMap<ObjectId, RgbaData> for frame data.
  </action>
  <verify>
<automated>cargo check -p wayland-remote-server 2>&1 | head -30</automated>
</verify>
  <done>ServerState has streaming_state field with surfaces HashMap for window_id -> FrameData mapping</done>
</task>

</tasks>

<verification>
[Phase 4 checks]
- TCP server can be constructed with configurable port
- Frame header encodes/decodes correctly with big-endian
- ServerState has streaming_state integrated
</verification>

<success_criteria>
- TCP server accepts port configuration (STREAM-01)
- Binary frame protocol sends header with correct fields (STREAM-02)
- Module compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-tcp-frame-streaming/04-01-SUMMARY.md`
</output>
