---
phase: 03-headless-rendering
plan: 02
type: execute
wave: 2
depends_on: ["03-headless-rendering-01"]
files_modified: [
  "crates/server/src/rendering/mod.rs",
  "crates/server/src/rendering/offscreen.rs",
  "crates/server/src/state.rs",
  "crates/server/src/handlers/compositor.rs"
]
autonomous: true
requirements: [REND-02]
must_haves:
  truths:
    - "Surface content is rendered to offscreen buffer"
    - "Per-surface offscreen buffer tracked in state"
    - "Render target is created and bound before rendering"
  artifacts:
    - path: "crates/server/src/rendering/offscreen.rs"
      provides: "Offscreen buffer management and rendering"
      contains: "render_surface_to_buffer"
    - path: "crates/server/src/rendering/mod.rs"
      provides: "Rendering module exports"
      contains: "pub mod offscreen"
    - path: "crates/server/src/state.rs"
      provides: "Per-surface buffer tracking"
      contains: "offscreen_buffers: HashMap<ObjectId, pixman::Image>"
  key_links:
    - from: "crates/server/src/handlers/compositor.rs"
      to: "crates/server/src/rendering/offscreen.rs"
      via: "CompositorHandler::commit()"
      pattern: "render_surface_to_buffer"
    - from: "crates/server/src/rendering/offscreen.rs"
      to: "PixmanRenderer::bind()"
      via: "Offscreen::create_buffer + Bind::bind"
      pattern: "renderer.bind"
---

<objective>
Implement surface rendering to offscreen buffers using PixmanRenderer.

Purpose: Render Wayland surface content to memory-backed framebuffers that can be read back as RGBA data for streaming.
Output: Rendering module with offscreen buffer creation and surface rendering functions.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-headless-rendering/03-RESEARCH.md
@crates/server/src/state.rs
@crates/server/src/handlers/compositor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rendering module structure</name>
  <files>
    crates/server/src/rendering/mod.rs
    crates/server/src/rendering/offscreen.rs
  </files>
  <action>
    Create the rendering module structure:
    
    1. Create crates/server/src/rendering/mod.rs with pub mod offscreen;
    2. Create crates/server/src/rendering/offscreen.rs with rendering logic
    3. Add mod rendering; to crates/server/src/lib.rs or main.rs
    
    The offscreen.rs will contain functions for buffer management and surface rendering.
  </action>
  <verify>
    <automated>test -f crates/server/src/rendering/mod.rs && test -f crates/server/src/rendering/offscreen.rs && echo "PASS: module created" || echo "FAIL: module missing"</automated>
  </verify>
  <done>Rendering module files exist and are included in lib.rs</done>
</task>

<task type="auto">
  <name>Task 2: Implement offscreen buffer creation and surface rendering</name>
  <files>crates/server/src/rendering/offscreen.rs</files>
  <action>
    Implement offscreen buffer creation and surface rendering in offscreen.rs:
    
    Key imports:
    - use smithay::backend::renderer::{Offscreen, Bind, Renderer, ImportMemWl, Frame};
    - use smithay::backend::renderer::pixman::PixmanRenderer;
    - use smithay::utils::Size;
    - use smithay::reexports::wayland_server::protocol::wl_surface::WlSurface;
    
    Functions to implement:
    1. create_offscreen_buffer(renderer, width, height) -> pixman::Image:
       - Use renderer.create_buffer(Fourcc::Abgr8888, Size::from((width, height)))
    
    2. render_surface_to_buffer(renderer, surface, buffer) -> Result:
       - Import buffer using ImportMemWl::import_shm_buffer()
       - Bind offscreen target using Bind::bind()
       - Use Renderer::render() to get PixmanFrame
       - Use Frame::render_texture_from_to() to composite surface
       - Call frame.finish()
    
    Reference Pattern 2 from RESEARCH.md for surface rendering.
  </action>
  <verify>
    <automated>cargo check -p wayland-remote-server 2>&1 | head -30</automated>
  </verify>
  <done>render_surface_to_buffer function compiles and accepts PixmanRenderer, WlSurface, and pixman::Image</done>
</task>

<task type="auto">
  <name>Task 3: Add per-surface buffer tracking to ServerState</name>
  <files>crates/server/src/state.rs</files>
  <action>
    Add offscreen buffer tracking to ServerState:
    
    1. Import pixman crate: use pixman::Image;
    2. Add field: pub offscreen_buffers: HashMap<ObjectId, Image<'static, 'static>>
    3. Initialize in new(): offscreen_buffers: HashMap::new()
    4. Import rendering functions in state.rs: use crate::rendering::offscreen::*;
    
    This HashMap tracks the offscreen buffer per surface for RGBA extraction.
  </action>
  <verify>
    <automated>cargo check -p wayland-remote-server 2>&1 | grep -E "(error|warning)" | head -10</automated>
  </verify>
  <done>ServerState has offscreen_buffers field with proper type and imports</done>
</task>

<task type="auto">
  <name>Task 4: Integrate rendering into commit handler</name>
  <files>crates/server/src/state.rs</files>
  <action>
    Modify CompositorHandler::commit() to render surfaces to offscreen buffers:
    
    In the commit() method after buffer_attached detection:
    1. Get surface geometry using with_states and SurfaceAttributes
    2. Create/get offscreen buffer for surface_id
    3. Call render_surface_to_buffer() with self.renderer, surface, and buffer
    4. Store buffer in self.offscreen_buffers
    
    Handle errors appropriately - log but don't panic if render fails.
  </action>
  <verify>
    <automated>cargo build -p wayland-remote-server 2>&1 | tail -20</automated>
  </verify>
  <done>commit() calls render_surface_to_buffer when buffer is attached, compiles successfully</done>
</task>

</tasks>

<verification>
- Rendering module exists with offscreen buffer creation
- ServerState tracks per-surface offscreen buffers
- commit() renders surfaces to buffers when buffers attached
- Compilation succeeds
</verification>

<success_criteria>
- render_surface_to_buffer function exists and compiles
- Offscreen buffer created using create_buffer(Fourcc::Abgr8888, size)
- Surface imported using ImportMemWl::import_shm_buffer
- Frame rendered with render_texture_from_to and finished with frame.finish()
- Per-surface buffer tracking works in ServerState
</success_criteria>

<output>
After completion, create `.planning/phases/03-headless-rendering/03-02-SUMMARY.md`
</output>
