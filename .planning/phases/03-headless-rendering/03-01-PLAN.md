---
phase: 03-headless-rendering
plan: 01
type: execute
wave: 1
depends_on: ["02-wayland-core-protocol-03"]
files_modified: [
  "crates/server/src/state.rs",
  "crates/server/Cargo.toml"
]
autonomous: true
requirements: [REND-01]
must_haves:
  truths:
    - "PixmanRenderer instance exists in ServerState"
    - "Server compiles with renderer_pixman feature"
    - "Offscreen buffer creation API is available"
  artifacts:
    - path: "crates/server/src/state.rs"
      provides: "PixmanRenderer field in ServerState"
      contains: "pub renderer: PixmanRenderer"
    - path: "crates/server/Cargo.toml"
      provides: "renderer_pixman feature enabled"
      contains: "renderer_pixman"
  key_links:
    - from: "crates/server/src/state.rs"
      to: "smithay::backend::renderer::pixman::PixmanRenderer"
      via: "new() with Offscreen trait"
      pattern: "PixmanRenderer::new"
---

<objective>
Initialize PixmanRenderer for headless software rendering.

Purpose: PixmanRenderer provides CPU-based rendering without GPU/display requirements, enabling true headless operation for offscreen framebuffers.
Output: ServerState with PixmanRenderer field, Cargo.toml with renderer_pixman feature.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-headless-rendering/03-RESEARCH.md
@crates/server/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add renderer_pixman feature to Cargo.toml</name>
  <files>crates/server/Cargo.toml</files>
  <action>
    Add the renderer_pixman feature to the smithay dependency in Cargo.toml:
    
    smithay = { version = "0.7.0", features = ["wayland_frontend", "renderer_pixman"] }
    
    This enables PixmanRenderer and Offscreen trait support in Smithay.
  </action>
  <verify>
    <automated>grep -q "renderer_pixman" crates/server/Cargo.toml && echo "PASS: renderer_pixman feature found" || echo "FAIL: feature not found"</automated>
  </verify>
  <done>Cargo.toml includes renderer_pixman in smithay features</done>
</task>

<task type="auto">
  <name>Task 2: Add PixmanRenderer to ServerState</name>
  <files>crates/server/src/state.rs</files>
  <action>
    Add PixmanRenderer field to ServerState:
    
    1. Import: use smithay::backend::renderer::pixman::PixmanRenderer;
    2. Add field: pub renderer: PixmanRenderer
    3. Initialize in new(): renderer: PixmanRenderer::new().expect("Failed to create PixmanRenderer")
    
    PixmanRenderer implements Offscreen<Image> trait for creating memory-backed framebuffers.
  </action>
  <verify>
    <automated>cargo check -p wayland-remote-server 2>&1 | head -20</automated>
  </verify>
  <done>ServerState has PixmanRenderer field, compiles without errors</done>
</task>

</tasks>

<verification>
- PixmanRenderer is initialized in ServerState::new()
- Server compiles with renderer_pixman feature
- Offscreen buffer creation API available via renderer.create_buffer()
</verification>

<success_criteria>
- PixmanRenderer exists in ServerState
- renderer_pixman feature enabled in Cargo.toml
- Compilation succeeds with cargo build -p wayland-remote-server
</success_criteria>

<output>
After completion, create `.planning/phases/03-headless-rendering/03-01-SUMMARY.md`
</output>
